# See https://pre-commit.com/ for usage and config
repos:
- repo: local
  hooks:
  - id: commit
    name: Commit
    stages: [commit]
    language: system
    entry: mdformat content
    pass_filenames: false
    verbose: false
